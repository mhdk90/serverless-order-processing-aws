AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Serverless order processing demo with API Gateway, Lambda, SQS, DynamoDB, and SNS

Globals:
  Function:
    Runtime: python3.12
    Timeout: 10
    MemorySize: 128

Resources:
  OrdersQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: orders-queue

  OrdersTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: orders-topic

  OrdersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: orders-table
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: orderId
          AttributeType: S
      KeySchema:
        - AttributeName: orderId
          KeyType: HASH

  SubmitOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/submit_order/
      Handler: app.handler
      Environment:
        Variables:
          QUEUE_URL: !Ref OrdersQueue
      Policies:
        - SQSSendMessagePolicy:
            QueueName: !GetAtt OrdersQueue.QueueName
      Events:
        SubmitOrderApi:
          Type: Api
          Properties:
            Path: /orders
            Method: post

  ProcessOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/process_order/
      Handler: app.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref OrdersTable
          TOPIC_ARN: !Ref OrdersTopic
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrdersTable
        - SNSPublishMessagePolicy:
            TopicName: !GetAtt OrdersTopic.TopicName
      Events:
        QueueTrigger:
          Type: SQS
          Properties:
            Queue: !GetAtt OrdersQueue.Arn
            BatchSize: 1

Outputs:
  ApiEndpoint:
    Description: API endpoint for submitting orders
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/orders"

  QueueName:
    Description: SQS queue name
    Value: !GetAtt OrdersQueue.QueueName

  TopicArn:
    Description: SNS topic ARN
    Value: !Ref OrdersTopic

  TableName:
    Description: DynamoDB table name
    Value: !Ref OrdersTable
